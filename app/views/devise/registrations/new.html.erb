<div class="container">
  <h2 class="new-user">新規会員登録</h2>

  <p　class="text">アニレビュは、280文字以上の感想と評価で探せるレビューサイトです。
  一緒にオススメのアニメを探しませんか？</p>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>


    <div class="field">
      <%= f.label :名前 %>
      <p class="need">必須</p>
      <p class="add">*12文字以内</p>
      <%= f.text_field :name, autofocus: true, maxlength: "12" %>
    </div>

    <div class="field">
      <%= f.label :メールアドレス %>
      <p class="need">必須</p>
      <p class="add">*半角英数字</p>
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="field">
      <%= f.label :パスワード %>
      <p class="need">必須</p>
      <p class="add">*8文字以上の半角英数字</p>
      <%= f.password_field :password, autocomplete: "new-password" %>
    </div>

    <div class="field">
      <%= f.label :"パスワード(再確認)" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
    </div>

    <div class="actions">
      <%= f.submit "登録する" %>
    </div>
  <% end %>
</div>